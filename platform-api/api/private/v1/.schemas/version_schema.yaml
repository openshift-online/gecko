properties:
  apiVersion:
    description: |-
      APIVersion defines the versioned schema of this representation of an object.
      Servers should convert recognized schemas to the latest internal value, and
      may reject unrecognized values.
      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    type: string
  kind:
    description: |-
      Kind is a string value representing the REST resource this object represents.
      Servers may infer this from the endpoint the client submits requests to.
      Cannot be updated.
      In CamelCase.
      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    type: string
  metadata:
    type: object
  spec:
    description: VersionSpec contains release information synchronized from Cincinnati.
    properties:
      channelGroups:
        description: ChannelGroups contains the Cincinnati channel groups containing
          this release.
        items:
          type: string
        minItems: 1
        type: array
        uniqueItems: true
      releaseImage:
        description: |-
          ReleaseImage is the Cincinnati release payload used internally by Gecko.
          It is intentionally not exposed through the public API.
        minLength: 1
        type: string
    required:
    - channelGroups
    - releaseImage
    type: object
required:
- spec
type: object
