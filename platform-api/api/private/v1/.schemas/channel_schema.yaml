properties:
  apiVersion:
    description: |-
      APIVersion defines the versioned schema of this representation of an object.
      Servers should convert recognized schemas to the latest internal value, and
      may reject unrecognized values.
      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    type: string
  kind:
    description: |-
      Kind is a string value representing the REST resource this object represents.
      Servers may infer this from the endpoint the client submits requests to.
      Cannot be updated.
      In CamelCase.
      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    type: string
  metadata:
    type: object
  spec:
    description: ChannelSpec contains platform-managed controls for a channel group.
    properties:
      fleetMinorVersion:
        description: |-
          FleetMinorVersion is the major.minor release line approved for automatic
          fleet upgrades.
        pattern: ^(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)$
        type: string
      installDefaultVersion:
        description: |-
          InstallDefaultVersion is the exact release selected when a client does
          not provide a version during cluster creation.
        minLength: 1
        type: string
    required:
    - fleetMinorVersion
    - installDefaultVersion
    type: object
required:
- spec
type: object
